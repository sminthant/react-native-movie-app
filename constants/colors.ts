/**
 * Shared UI colors that must be visible in both light and dark mode.
 * Use these for logout and other critical actions so theme/overrides cannot hide them.
 */

export const LOGOUT = {
  background: "#991B1B",
  text: "#FFFFFF",
  border: "#7F1D1D",
} as const;
